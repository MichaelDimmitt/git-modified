find every git repo
run 
git status -s; 

on the list


git caching ... next to git config.

git clone maps an additional command to add to cache.

rm maps an additional command to remove from cache.
